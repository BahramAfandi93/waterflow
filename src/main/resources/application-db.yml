spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/flow-master_db
    username: user
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true